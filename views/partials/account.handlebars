{{#if user}}
<h1 class='fname'>Hello, {{user.first_name}} {{user.last_name}}</h1>
<div class='balance'>
    <p>Account Number: {{user.account_number}}</p>
    <p>Balance: {{user.current_balance}}</p>
</div>
<div class='transactions-box'>
    <p>box full of transactions</p>
</div>
{{else}}
{{> setupAccount}}
{{/if}}